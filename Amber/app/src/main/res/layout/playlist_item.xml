<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ImageView
        android:layout_width="@dimen/material_component_lists_two_line_height"
        android:layout_height="@dimen/material_component_lists_two_line_height"
        app:srcCompat="@color/hint_text_on_light_background"
        android:id="@+id/playlist_item_image_view" />

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="@dimen/material_component_lists_two_line_height"
        android:padding="5dp">

        <TextView
            android:id="@+id/playlist_item_title_text_view"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textColor="@color/primary_text_on_light_background"
            android:text="Title" />

        <TextView
            android:id="@+id/playlist_item_subtitle_text_view"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textColor="@color/hint_text_on_light_background"
            android:text="Subtitle" />

    </LinearLayout>
</LinearLayout>
