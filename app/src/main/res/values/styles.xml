<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColor">@color/primary_text_on_light_background</item>
        <item name="android:textColorHint">@color/hint_text_on_light_background</item>
        <item name="android:textColorSecondary">@color/secondary_text_on_light_background</item>
    </style>

</resources>
